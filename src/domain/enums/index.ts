export enum RideStatus {
  REQUESTED = 'REQUESTED',
  ONGOING = 'ONGOING',
  COMPLETED = 'COMPLETED',
  CANCELLED = 'CANCELLED',
}

export enum DriverStatus {
  AVAILABLE = 'AVAILABLE',
  ON_RIDE = 'ON_RIDE',
  OFFLINE = 'OFFLINE',
}

export enum VehicleType {
  HATCHBACK = 'HATCHBACK',
  SEDAN = 'SEDAN',
}
